import { ExternalrouterdirectiveDirective } from './externalrouterdirective.directive';

describe('ExternalrouterdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ExternalrouterdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
